import { IconButton } from './IconButton/IconButton';

export const App = () => {
  return (
    <div>
      Trenning
      <IconButton variant="primary" size="lg">
        Click me
      </IconButton>
    </div>
  );
};
